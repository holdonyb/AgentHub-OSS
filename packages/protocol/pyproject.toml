[project]
name = "agenthub-protocol"
version = "0.1.0"
description = "Shared AgentHub protocol models"
requires-python = ">=3.10"
dependencies = ["pydantic>=2.7,<3.0"]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
